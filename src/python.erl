%% -*- erlang-indent-level: 2 -*-
%%------------------------------------------------------------------------------
-module(python).

-export([solve/3]).

-spec solve([{file:filename(), integer(), integer(), boolean()}], [cuter_symbolic:mapping()], string()) -> error.
solve(Fs, Ms, Python) -> error.
